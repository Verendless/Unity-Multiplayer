using Network;
using TMPro;
using UnityEngine;

public class JoinCodeText : MonoBehaviour
{
    [SerializeField] private TextMeshProUGUI joinCode;
    // Start is called before the first frame update
    // void Start()
    // {
    //     joinCode.text = $"Join Code: {HostSingleton.Instance.HostGameManager.JoinCode}";
    // }
}
